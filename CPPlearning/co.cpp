#include <bits/stdc++.h>
using namespace std;
int main()
{
    int a = 3, b = -6, c = 12;
    int result;
    result = (a * a + b * b + c * c) + 2 * (a * b + b * c + a * c);
    cout << result;
    return 0;
}