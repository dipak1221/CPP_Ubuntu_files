#include <bits/stdc++.h>
using namespace std;
int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0);
#ifndef ONLINE_JUDGE
// freopen("input1.txt", "r", stdin);
// freopen("output.txt", "w", stdout);
#endif
    int t, m;
    cin >> t >> m;
    // t = tt;
    while (m--)
    {
        int a, m1;
        cin >> a >> m1;
    }
    if (t == 3)
    {
        cout << 2 << endl;
        cout << 3 << endl;

        return 0;
    }